services:
  pg-db:
    image: postgres:15.1-alpine
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=imigz 
      - POSTGRES_USER=admin
    ports:
      - 5432:5432
    volumes:
      - pgData:/var/lib/postgresql

volumes:
  pgData: